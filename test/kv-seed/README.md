to run the tests, we need to seed KV with type declaration files for
any of the libraries we depend on in the test cases.

this folder can contain files that the values we would like to seed KV with for various dependencies.

just need to get json imports working with vitest + cloudflare